import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ph2.u2.gonzalovillagra',
  appName: 'ph2-u2-gonzalovillagra',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
